{
  "name": "bouncing-balls",
  "version": "1.0.0",
  "description": "First attempt using canvas, making bouncing balls",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css": "node-sass src/scss/style.scss -o dist",
    "css:watch": "npm run css && node-sass src/scss/style.scss -wo dist",
    "start": "npm run css && node-sass src/scss/style.scss -wo dist && tsc -w"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hubofhugo/bouncing-balls.git"
  },
  "author": "Hugo Brook",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/hubofhugo/bouncing-balls/issues"
  },
  "homepage": "https://github.com/hubofhugo/bouncing-balls#readme",
  "devDependencies": {
    "@types/jest": "^20.0.4",
    "@types/tape": "^4.2.30",
    "jest": "^20.0.4",
    "node-sass": "^3.4.2",
    "tape": "^4.7.0",
    "ts-jest": "^20.0.7",
    "typescript": "^2.4.2"
  }
}
